# ProcessKiller (`src/main/kill`)

Windows blocklist terminator. Session-wiring should import `createProcessKiller` and pass live allowlist matchers.

See [PROBE.md](./PROBE.md) for the gauntlet command, safety invariant, and IPC seam.
