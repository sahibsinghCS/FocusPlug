# Independent critic (faces)

Blind critic (no builder rationale). Two bars:

1. **Foundation host** — 1280×800 Session after vs a blank/Decision-only hero; Hourglass + Readout must read as instruments.
2. **Cheap-wow pack** — at 1280×800, Descent / Orbit / Circuit must each beat a plain progress bar at the same progress. Circuit must read as FocusPlug-native (fuse / plug / kill rail), not a hobby schematic.

## Foundation host — Round 1 WIN

- **Selected home:** AFTER
- **Largest remaining gap:** Filmed mid-transfer hourglass (`progress=0.62`) while elapsed was still `00:05` — posed atmosphere vs locked progress. Honest live still is near-0% transfer. Readout linear track was easy to miss in the first stills.
- **Hourglass:** Dark-room sand-transfer object (full top at rest, split chambers mid-block), not a void-lost icon.
- **Readout:** Honest digits + remaining/block/kills/clock strip. Track restyled thicker after this note.

Stills: `after-idle-readout`, `after-live-readout`, `after-idle-hourglass`, `after-live-hourglass`, `after-mid-hourglass`, `after-live-flight`, `after-settings`.

## Cheap-wow pack — WIN (standalone A/B)

Stills compared: `bar-062` vs `descent-062` / `orbit-062` / `circuit-062`, plus lock/abyssal and `vs-*` A/B pairs in this folder.

- **Descent:** selected over the bar. Midnight at 2,500 m is a place; abyssal is a darker place.
- **Orbit:** selected over the bar. Mid-session is the thinnest of the three; lock (stacked bodies + closed lime ring) is a finished object.
- **Circuit:** selected over the bar and reads as FocusPlug (outlet, glass fuse, kill rail, red ARM / lime LOCK).

**Largest remaining ding (not a loss):** Orbit at 62% still leans on the numeral more than Descent does.

**Haven / AGPL:** original FocusPlug faces. No Haven source or assets.

**Checks:** `npm test` and `npm run typecheck` pass.

## Cheap-wow on foundation host

Rebase onto merged foundation (PR #26). New Session stills: `after-live-descent`, `after-live-orbit`, `after-live-circuit`, plus lock frames. Verdict after recapture.
