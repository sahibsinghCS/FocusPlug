# Faces foundation evidence

Stills for the independent critic. Prefix `after-` is this stream.

```bash
npx vite --config scripts/renderer-preview.vite.ts
# then capture 1280×800:
# after-idle-readout
# after-live-readout
# after-live-hourglass
# after-live-flight
```
