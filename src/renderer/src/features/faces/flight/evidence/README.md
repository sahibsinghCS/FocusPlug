# Flight face v2 stills

1280×800 captures for the gauntlet.

- BEFORE = Timmy's hated plate (`before-timmy-grid-1280x800.png`) plus the flat sticker.
- AFTER = zoomed, orbiting, no-grid instrument with a choosable DUB→EDI (and JFK→LHR) route.

```bash
npx vite --config scripts/renderer-preview.vite.ts
node scripts/flight-stills.mjs
```

Pinned UTC: `2026-09-12T16:00:00Z` (day Ireland) and `2026-09-12T02:00:00Z` (night lights).
Paired cruise stills at `idle=0.35` and `idle=1.55` prove the globe actually rotated.
