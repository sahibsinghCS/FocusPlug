# Independent critic (faces-candle)

Blind critic (no builder rationale). Bar: 1280×800 **start / mid / near-end** stills must read as a **melting candle timer**, not a flat yellow stick, and must **WIN** vs a plain progress bar and vs leftover Field. WIN / LOSE only.

Stills compared: `after-start-004-1280x800.png`, `after-mid-050-1280x800.png`, `after-end-092-1280x800.png` vs `host-bar-1280x800.png`. Host check: `host-start-1280x800.png`, `host-mid-1280x800.png`, `host-end-1280x800.png`.

## Round 1 — LOSE

- **VERDICT:** LOSE
- **Why:** Cream cylinder with a pin flame. Drips were hairlines. Pool was a ring. Read as Field's yellow stick, not a burn.

## Round 2 — WIN

- **VERDICT:** WIN
- **Selected:** Candle over the bar. Start / mid / end over any leftover Field column.
- **Largest remaining ding (not a loss):** Isolated plate is still illustration-grade (smooth gold fill, thin drip runs) rather than photographed beeswax. The melt is obvious; the material is drawn.

**Why WIN:**

- START is a tall tapered pillar, just-lit crater, one forming drip, layered flame and glow. 96% wax.
- MID is clearly shorter. Both sides carry wax runs with teardrop bulbs. The rim is a melted lip, not a flat cap. 50% wax.
- END is a stub in a pooled puddle. Drips have reached the base. 8% wax. Elapsed ↑ → wax ↓ is immediate.
- The bar still is a lime track and a percentage. Nobody films that over the candle.
- Field stays retired. Host picker reads **Candle**; the same pillar shortens across `host-start` / `host-mid` / `host-end`.
- Stakes still (`after-stakes-kills`) keeps the same mid melt and adds a smoke wisp + SPUTTER — not a broken mesh.

**Haven / AGPL:** original FocusPlug face. No Haven source or assets.

**Checks:** `npm test` (322) and `npm run typecheck` pass. `faceId: "candle"` persists on the settings blob. Retired `field` still rejects.
