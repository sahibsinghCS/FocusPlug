# Hourglass v2 — independent critic

Blind critic (no builder rationale). Bar: 1280×800 BEFORE vs AFTER. AFTER must win as a beautiful dark-room instrument, not a geometry demo.

## Round 1 — WIN

Stills compared:

- BEFORE: `before-live-1280x800.png` (rejected triangles / 1px stream)
- AFTER live: `after-live-hero-1280x800.png`
- AFTER mid: `after-mid-hero-1280x800.png`
- AFTER done: `after-done-hero-1280x800.png`

- **Selected:** AFTER
- **Verdict:** WIN
- **Largest remaining gap:** The fall still reads as a stiff gold pin rather than a grainy, tapered sand ribbon, so the transfer feels illustrated instead of physical.

Post-verdict: stream redrawn as a tapered halo + grain field (not a constant-width bar). Host stills unchanged in contract.

**Checks:** `npx vitest run` 300/300, `npm run typecheck` clean.
