# configuration-ux critic

**Verdict:** WIN  
**Blind A/B:** Variant A = `main` @ `ad26d4c` (pre-Phase-3 config pages). Variant B = this branch. Critic was not told which was new.

**Bar:** Fresh harsh critic picks which config surface to ship for effortless + trustworthy allow/block, plugs, and desk-model settings at 1280×800.

**Chosen:** B

**Evidence:** chips + default callouts vs comma blobs; protocol cards + never-study-PC + LAN steps vs a protocol dropdown; BlazeFace/Custom/Stub cards + unprobed custom readiness vs a single select.

**Haven / AGPL:** original FocusPlug UI. No Haven source or assets.

**Checks:** `npm run typecheck` and `npm test` (178) pass. Add/toggle/remove plug and `deskSetModelId` remain on frozen IPC.
