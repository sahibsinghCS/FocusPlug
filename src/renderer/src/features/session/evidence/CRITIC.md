# Independent critic (session-command-center)

Blind critic (no builder rationale). Bar: 1280×800 before/after; select the new Session home **and** kill overlay as the Devpost/demo hero; must-haves readable without zoom.

## Round 1 — WIN

- **Selected home:** AFTER
- **Selected overlay:** AFTER
- **Largest remaining risk:** Projector softness on recovered event rows and “+N” list chips; overlay headline is still “Killing blocked apps in” while plug-cut sits in the secondary cards.

Stills compared: `before-*` vs `after-*` in this folder (idle, on-task, away, recovered, overlay). After idle/on-task/away put IDLE / ON TASK / AWAY in billboard type; after recovered is the only home still that reads as a film (CAUSE / CONSEQUENCE / RECOVERY + KILL · PLUG_OFF · UNLOCK). After overlay keeps the `08` ring and adds APP KILL / PLUGS CUT.
